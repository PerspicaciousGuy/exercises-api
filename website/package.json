{
  "name": "exercisedb-api-docs",
  "version": "0.1.0",
  "description": "Documentation site for the ExerciseDB API.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "sync:spec": "node scripts/sync-spec.js",
    "predev": "npm run sync:spec",
    "dev": "vitepress dev",
    "prebuild": "npm run sync:spec",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "@scalar/api-reference": "1.62.5",
    "vitepress": "1.6.4",
    "vue": "3.5.39"
  }
}
